Execute the implementation tasks.

**Directives:**
1.  **Build First**: Fix the `index.tsx` links and `vercel.json` first.
2.  **Preserve Logic**: When refactoring `ChatBot.tsx`, keep the existing `chatWithBackend` calls that use the current API structure.
3.  **UI Polish**: Ensure the floating chat bubble has a high z-index (`z-50`) to sit above documentation.

