Generate dependency-ordered tasks for the integration.

**Task Organization:**

* **Phase 1: Backend (Neon Postgres)**
    * Task: Add `psycopg2-binary` and `sqlalchemy` to `requirements.txt`.
    * Task: Create `utils/database.py` with the SQLAlchemy engine configured for the Neon connection string.
    * Task: Create `utils/sql_models.py` (define `ChatHistory` table).
    * Task: Update `api/index.py` to persist chats to Postgres.

* **Phase 2: Frontend (Chat)**
    * Task: Create `src/theme/Root.tsx` to wrap the app with the Chatbot component.
    * Task: Update `src/lib/chatApi.ts` to handle session IDs and fetch history.

* **Phase 3: Localization (Urdu)**
    * Task: Update `docusaurus.config.ts` with `i18n` settings for Urdu (RTL).
    * Task: Update `src/components/HomepageFeatures/index.tsx` to use `<Translate>`.
    * Task: Run the setup command to scaffold the `i18n/ur` directory.

**Crucial Note**: The database tasks MUST NOT fallback to SQLite. If the connection fails, the app should error out (fail fast).